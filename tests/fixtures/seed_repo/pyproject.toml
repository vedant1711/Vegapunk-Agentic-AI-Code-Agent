# Minimal pyproject so tools/test_runner.py auto-detects pytest as the
# test framework for this fixture.

[project]
name = "seed-repo"
version = "0.0.1"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
