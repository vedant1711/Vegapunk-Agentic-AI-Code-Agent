"""Reproducible benchmark suite for Vegapunk.

Every measurement in docs/benchmarks.md and every claim on the
/architecture page traces back to a script in here. `make bench` runs
all of them, writes `benchmarks/results.json`, and updates the doc.
"""
